{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "msedge",   
            "request": "launch",
            "name": "Open index.html",
            "file": "${workspaceFolder}/index.html" // เปลี่ยนจาก url เป็น file
        }
    ]
}